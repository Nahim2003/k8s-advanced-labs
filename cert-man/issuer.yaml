apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: issuer
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: "nahimahmed1000@gmail.com"
    privateKeySecretRef:
      name: issuer
    solvers:
    - dns01:
        route53:
          hostedZoneID: Z0046726Q5UO6LL3MW4R
          region: eu-west-2
      selector:
        dnsZones:
        - "lab.nahim.co.uk"
    - http01:
        ingress:
          class: nginx   